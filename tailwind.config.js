/** @type {import('tailwindcss').Config} */
module.exports = {

  content: [
    './src/**/*.{html,js}', // Include all relevant file types in the src folder
    './src/**/*.{html,jsx}' // Ensure proper glob pattern for the package
  ],
  
  theme: {
    extend: {},
  },
  plugins: [],
}
